# V-World(국토교통부) 인증키 — https://www.vworld.kr 에서 발급
# 발급 시 등록한 도메인에서만 동작한다. 배포하면 그 도메인을 추가할 것.
VITE_VWORLD_KEY=

# ── 배포(Vercel) ──
# 서버 함수만 보는 값이라 번들에 들어가지 않는다. 넣어 두면 위의 VITE_ 키는 비워도 된다.
VWORLD_KEY=
# 인증키에 등록한 도메인. 비우면 배포된 호스트를 그대로 쓴다.
VWORLD_REFERER=
