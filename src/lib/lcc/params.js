/**
 * LCC 계산 파라미터 — 식은 백병훈·조중연(2014) 식(1)을 따른다.
 *
 *          T
 *  C_TOT = C_INI + Σ  ──────── × ( C_MAI(t) + C_DIS(t) )
 *         t=1   (1+q)^t
 *
 * 여기 두 값(분석기간 T, 할인율 q)은 지어낸 것이 아니라 근거가 있는 값만 싣는다.
 * 근거가 확정되지 않은 값은 status 로 표시해 화면이 그대로 드러내게 한다.
 */

const K = (ko, it) => ({ ko, it })

/**
 * 분석기간 T — 몇 년을 내다보는가.
 * 논문·ISO 15686-5·국내 관행이 모두 50년으로 모인다.
 */
export const HORIZON = {
  years: 50,
  from: 1989,            // 준공년. t=0 기준은 계산 시 NOW 로 옮긴다
  status: 'confirmed',
  source: 'ISO 15686-5 · 백병훈·조중연(2014) 분석기간 50년 · 국내 관행 40~50년',
  label: K('분석기간 50년', 'Orizzonte 50 anni'),
}

/**
 * 할인율 q — 오늘의 1억과 30년 뒤 1억을 같은 자로 재기 위한 값.
 *
 * 논문은 4.5% 를 썼다. 공공사업의 정본은 기획재정부 예비타당성조사 지침의
 * 사회적 할인율이다. 현행 수치를 원문에서 확인하기 전까지는 estimate 로 둔다 —
 * 이 값이 결과를 가장 크게 흔들기 때문에(논문도 민감도 분석을 요구한다) 함부로 확정하지 않는다.
 */
export const DISCOUNT = {
  rate: 0.045,
  status: 'estimate',
  source: '백병훈·조중연(2014) 적용값 4.5% — 현행값은 기재부 예비타당성조사 수행 총괄지침에서 확인 필요',
  label: K('할인율 4.5% (잠정)', 'Tasso di sconto 4,5% (provvisorio)'),
  /** 결과가 여기에 민감하므로 반드시 이 범위로 민감도 분석 */
  sensitivity: [0.03, 0.045, 0.055],
}

/** 금액 표기 단위 — 억원 */
export const UNIT = K('억원', 'mld di won')
